[package]
name = "aios-design-tokens"
description = "AIOS L7 shared design tokens — one closed, typed token vocabulary (color/spacing/radius/typography, Light+Dark) emitted identically to Web CSS custom properties and KDE QML properties, with an emitter-equivalence parity proof."
edition.workspace = true
rust-version.workspace = true
version = "0.1.0"
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
serde        = { workspace = true }
strum        = { workspace = true }
strum_macros = { workspace = true }

[lints]
workspace = true
